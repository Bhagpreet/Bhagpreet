import { RippleOnHoverDirective } from './ripple-on-hover.directive';

describe('RippleOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new RippleOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
